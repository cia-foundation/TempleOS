void DotNet()
{
  I8 i;
  GrBitMap *base=GrNew(grbase);
  PrintF("$$CL$$");  //Clear text
  base->color=GR_RED^GR_WHITE; //Printed on white background
  for (i=0;i<600;i+=20)
    GrLine(base,i,0,0,600-i,3);
}

DotNet;  //Run when #included
//Type "GrClear;" to get rid of it